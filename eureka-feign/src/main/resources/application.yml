server:
  port: 8765
  tomcat:
    basedir: I:/temp/cloud

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: eureka-feign
#  profiles:
#    active: dev
    
#logging:
#  config: classpath:logback-cloud.xml
